# requirements.txt — local dev / CI (installs everything)
# [AUDIT High #1 FIX]: Exact version pins (==) prevent silent breakage on
# next major release. PyTorch 3.0, FastAPI 1.x, MLflow 3.x are active targets.
# Use pip-compile to regenerate: pip install pip-tools && pip-compile requirements.in
#
# NOTE: PyTorch wheel selection:
#   CPU:  pip install torch==2.2.1
#   CUDA 11.8: pip install torch==2.2.1 --index-url https://download.pytorch.org/whl/cu118
#   CUDA 12.1: pip install torch==2.2.1 --index-url https://download.pytorch.org/whl/cu121
# This file installs the CPU wheel by default (CI-safe).
# Hospital GPU deployment: set the correct index URL in Dockerfile.api.

torch==2.2.1
numpy==1.26.4
pandas==2.2.1
scikit-learn==1.4.1
joblib==1.3.2
matplotlib==3.8.3
seaborn==0.13.2
streamlit==1.32.2
tqdm==4.66.2
mlflow==2.10.1
fastapi==0.110.0
uvicorn==0.29.0
prometheus-client==0.20.0
python-multipart==0.0.9
pydantic==2.6.4
slowapi==0.1.9
httpx==0.27.0
